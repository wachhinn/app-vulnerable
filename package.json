{
  "name": "app-vulnerable",
  "version": "1.0.0",
  "description": "Aplicación web vulnerable para pruebas",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "security-scan": "echo \"Running security checks...\""
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
